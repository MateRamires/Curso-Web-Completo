console.log('Teste'); //ctrl + alt + n = Executa o codigo.

//ctrl + / = Comenta todas as linhas selecionadas.

//alt + shift + seta para baixo = Duplica a linha atual.

//ctrl + x = Recorta a linha atual. Pode ser usado para excluir alinha alternativamente. 

var quantidade = 10; //Inicializacao de variaveis.

let preco = 10; //Outra maneira de criar variaveis (a mais atual).

console.log(typeof preco); //Typeof mostra o tipo da variavel. Obs: ints e doubles sao considerados number em JS. 

const a = 3; //Declaracao de constantes em JS (Variaveis cujo valores nao podem ser alterados).

